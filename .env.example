# PostgreSQL connection for a future hosted deployment (the demo runs from local sample data)
DATABASE_URL="postgresql://user:password@localhost:5432/bloodlink"
AUTH_SECRET="replace-with-a-long-random-secret"
