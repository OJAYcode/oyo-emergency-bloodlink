import { currentUser } from "@/lib/auth";import { redirect } from "next/navigation";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){if(await currentUser())redirect('/dashboard');const q=await searchParams;return <main className="login-wrap"><section className="login-card"><div className="brand" style={{color:'#102a43',padding:0}}><span className="brand-mark" style={{background:'#0d67d8',color:'white'}}>✚</span><span>Oyo Emergency<br/>BloodLink</span></div><h1 style={{marginTop:25}}>Welcome back</h1><p>Sign in to the prototype coordination dashboard.</p>{q.error&&<p style={{color:'#b2182d',marginTop:12}}>{q.error}</p>}<form action="/api/auth/login" method="post"><div className="field"><label>Email</label><input required name="email" type="email" placeholder="you@bloodlink.demo"/></div><div className="field"><label>Password</label><input required name="password" type="password" placeholder="••••••••"/></div><button className="btn" type="submit">Sign in</button></form><div className="demo-box" style={{marginTop:18}}><strong>Demo accounts</strong><br/>Admin: admin@bloodlink.demo / Admin123!<br/>Hospital: oyo.central@bloodlink.demo / Hospital123!</div><p className="subtle" style={{marginTop:15}}>Prototype — sample data only. No patient data is collected.</p></section></main>}
