import { AppShell, Disclaimer } from "@/components/app-shell";import { currentUser } from "@/lib/auth";import { bloodGroups } from "@/lib/types";import { getStore } from "@/lib/store";import { redirect } from "next/navigation";
export default async function NewRequest({searchParams}:{searchParams:Promise<{blood?:string;hospital?:string;success?:string;matches?:string;error?:string}>}){const user=await currentUser();if(!user)redirect('/login');if(user.role==='ADMIN')redirect('/admin');const q=await searchParams;const store=await getStore();return <AppShell active="/requests/new"><div className="page"><div className="page-head"><div><h1>Request blood</h1><p>Send an emergency request to matching participating hospitals.</p></div></div>{q.success&&<div className="success-msg"><strong>Emergency blood request sent.</strong><br/>Request ID: {q.success} · Current status: PENDING · {q.matches||0} matching hospital alert(s) created.</div>}{q.error&&<div className="notice warning">{q.error}</div>}<form action="/api/requests" method="post" className="card form-card"><div className="form-grid"><div className="field"><label>Blood group *</label><select name="bloodGroup" required defaultValue={q.blood||''}><option value="" disabled>Select blood group</option>{bloodGroups.map(x=><option key={x}>{x}</option>)}</select></div><div className="field"><label>Quantity (units) *</label><input required name="quantity" min="1" max="99" type="number" placeholder="e.g. 3"/></div><div className="field"><label>Urgency *</label><select name="urgency" defaultValue="EMERGENCY"><option>EMERGENCY</option><option>URGENT</option><option>NORMAL</option></select></div><div className="field"><label>Preferred hospital (optional)</label><select name="preferredHospital" defaultValue={q.hospital||''}><option value="">Any matching hospital</option>{store.hospitals.filter(h=>h.active&&h.id!==user.hospitalId).map(h=><option value={h.id} key={h.id}>{h.name}</option>)}</select></div><div className="field"><label>Contact person *</label><input required name="contactPerson" placeholder="Name / role"/></div><div className="field"><label>Contact phone *</label><input required name="contactPhone" pattern="[0-9+\- ]{7,20}" placeholder="0800-000-0000"/></div><div className="field wide"><label>Additional note *</label><textarea required name="note" defaultValue="Urgent requirement for surgery" maxLength={280}/></div></div><div className="notice warning" style={{margin:'18px 0'}}>Do not include patient names, IDs, diagnoses, or other personal medical information.</div><button className="btn danger" type="submit">Send emergency request</button></form><div style={{marginTop:18}}><Disclaimer/></div></div></AppShell>}
